# Environment Detection and Configuration for Authentication
# Add these variables to your production environment

# For local development
NODE_ENV=development
NEXTAUTH_URL=http://localhost:3000

# For production deployment (replace with your actual domain)
# NODE_ENV=production
# NEXTAUTH_URL=https://yourdomain.com

# Security Configuration
# Set to 'false' to disable strict cookie security for testing
NEXTAUTH_STRICT_COOKIES=true

# Rate Limiting Configuration
# Increase these values if you have users behind shared IPs
MAX_LOGIN_ATTEMPTS_PER_EMAIL=5
MAX_LOGIN_ATTEMPTS_PER_IP=20

# Session Configuration
SESSION_MAX_AGE=14400  # 4 hours in seconds

# Debug Authentication Issues
DEBUG_AUTH=false